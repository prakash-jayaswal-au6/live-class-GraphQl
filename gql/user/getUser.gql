query user($id: String!) {
  user(id: $id) {
    id
    name
    role
    phone
    currentBalance
    otp
    walletId
    children
    parent
    products
    referralCode
    referedFrom
    referedUsers
  }
}
